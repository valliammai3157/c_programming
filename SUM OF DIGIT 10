#include<stdio.h>
#include<math.h>
#include<stdlib.h>

int main()
{
int n,sum=0;
    scanf("%d",&n);
    if(n<100){
        printf("Invalid Input\n");
    return 0;
}
while(n>0)
{
    sum+=n%10;n=n/10;
}
    printf("Sum of digit is %d\n",sum);
return 0;
}
